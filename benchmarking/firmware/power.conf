# Source Meter builds have no nRF52840 USB connection. Structured benchmark
# output is carried by BCTL1 frames over BLE L2CAP, so serial/printk support
# would only consume RAM and flash.
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_SERIAL=n
CONFIG_PRINTK=n
CONFIG_THREAD_ANALYZER_USE_PRINTK=n
